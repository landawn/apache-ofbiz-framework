===========================================================================================================

Generate Java entity classes annotated with Lombok and corresponding DAO classes based on the table definitions in mysql_ddl.sql.

References and examples: 	
	Abacus-jdbc APIs: ./docs/abacus-jdbc

Sample DAO implementations:
	C:\Users\haiyangl\Landawn\abacus-jdbc
	C:\Users\haiyangl\Landawn\abacus-jdbc\samples
	C:\Users\haiyangl\Landawn\abacus-jdbc\src\test\java
	C:\Users\haiyangl\Landawn\abacus-core
	

===========================================================================================================

Modify generate-persistence-from-mysql-ddl.gradle so that, if x.java already exists, it reads all lines starting from (and including)
// === Manually added fields === before regenerating x.java, and appends those lines to the newly generated x.java immediately before the final closing brace (}).

Note: x must remain lowercase; do not change x.java to X.java.


===========================================================================================================

Scan all Java classes and replace all string literals used in calls to GenericValue.getXXX(...), GenericValue.setXXX(...), and context.get(...) / context.put(...), where context is a variable or method parameter of type Map<String, ? extends Object>, with the corresponding fields defined in x.java.

Use only x.fieldName in the code (do not prefix with a package name). Ensure that x is properly imported in the class header.
If a referenced string literal does not exist in x.java, add a new field under the following comment: "// === Manually added fields ==="


Notes:
- x must remain lowercase; do not rename x.java to X.java.
- Save all generated scripts under folder: ./tools/ai_generated_scripts/.
- Do NOT delete the generated scripts after the task completes.


===========================================================================================================

Follow the steps below strictly:

Refactor all methods in all xxxServices classes to replace input parameters of type
"Map<String, ? extends Object> context" or "ServiceContext context" with strongly typed entity classes from
the org.apache.ofbiz.persistence.entity package.

Requirements:
- If no appropriate entity class exists in the org.apache.ofbiz.persistence.entity
  package, create a new model class in the org.apache.ofbiz.model package.
- Newly added classes in the org.apache.ofbiz.model package must NOT include the
  "Entity" suffix in their class names.
- Don't use "Map<String, ? extends Object> context" in any modified or new code.
- Avoid using bridge classes whenever possible.

Notes:
- Save all generated scripts under folder: ./tools/ai_generated_scripts/.
- Do NOT delete the generated scripts after the task completes.
	
	
===========================================================================================================



