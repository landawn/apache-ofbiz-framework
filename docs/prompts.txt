Migrate the project to Spring Boot and Spring Web, and refactor the persistence layer to use abacus-jdbc.

Dependencies to include:
- org.springframework.boot:spring-boot-starter-web:4.0.2
- org.springframework:spring-core:7.0.7
- com.landawn.abacus:abacus-common:7.7.2
- com.landawn.abacus:abacus-jdbc:4.7.2

Requirements:
1. Rewrite the project using Spring Boot with Spring Web.

2. Replace the entire persistence / data-access layer with abacus-jdbc.
   2.1 Rewrite all entity classes using Lombok annotations
       (e.g., @Data, @Getter/@Setter, @Builder where appropriate).
   2.2 Create a dedicated DAO class for each entity / database table.
   2.3 Refactor all persistence-related code so that:
       - All database access is performed exclusively through DAO classes.
       - No SQL, JDBC, or database-related logic exists outside DAO classes.
       - All calls to the persistence layer are replaced with calls to DAO classes.
       - If required functionality is not provided by
         com.landawn.abacus.jdbc.dao.Dao or
         com.landawn.abacus.jdbc.dao.CrudDao,
         add the necessary methods to the corresponding DAO class.

3. Do NOT introduce other ORM frameworks (e.g., JPA, Hibernate, MyBatis).

4. Keep existing business logic unchanged unless required for integration.

5. Ensure the application remains functionally equivalent after refactoring.

Reference documentation and samples:
- For abacus-common APIs, refer to: ./docs/abacus-common
- For abacus-jdbc APIs, refer to: ./docs/abacus-jdbc
- Reference DAO implementations can be found at:
    - C:\Users\haiyangl\Landawn\abacus-jdbc
    - C:\Users\haiyangl\Landawn\abacus-jdbc\samples
    - C:\Users\haiyangl\Landawn\abacus-jdbc\src\test\java

Constraints:
- Use only APIs that exist in the specified versions.
- Do not invent methods or classes.
- If an API usage is unclear, ask for clarification instead of guessing.

Output:
- Updated project structure
- Revised configuration files
- Refactored persistence code using abacus-jdbc
- Brief explanation of key changes