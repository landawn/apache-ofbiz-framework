server:
  port: 8080

spring:
  application:
    name: ofbiz-spring-web

logging:
  level:
    # Suppress noisy INFO logs when a TLS handshake is mistakenly sent to the HTTP port.
    org.apache.coyote.http11.Http11Processor: ERROR
